DATABASE_URL=postgres://myuser:mypassword@localhost:5432/node_db
PORT=4000

# Docker specific configurations (useful if other services need them)
POSTGRES_USER=myuser
POSTGRES_PASSWORD=mypassword
POSTGRES_DB=node_db
